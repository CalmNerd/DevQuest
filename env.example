# ===========================================
# DevQuest Environment Configuration Example
# ===========================================
# Copy this file to .env.local and fill in your actual values

# ===========================================
# GitHub OAuth Configuration
# ===========================================
# Get these from: https://github.com/settings/applications/new
GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret
GITHUB_CALLBACK_URL=http://localhost:3000/api/auth/github/callback

# Public GitHub Client ID (for client-side OAuth)
NEXT_PUBLIC_GITHUB_CLIENT_ID=your_github_oauth_client_id

# ===========================================
# GitHub API Token (Fallback)
# ===========================================
# Personal Access Token for app-level API calls
# Get from: https://github.com/settings/tokens
# Required scopes: read:user, user:email, repo, read:org
GITHUB_TOKEN=ghp_your_personal_access_token_here

# ===========================================
# Token Encryption
# ===========================================
# Generate with: openssl rand -hex 32
# This key encrypts user OAuth tokens in the database
ENCRYPTION_KEY=your_32_byte_hex_encryption_key_here

# ===========================================
# Database Configuration
# ===========================================
# PostgreSQL connection string
DATABASE_URL=postgresql://username:password@localhost:5432/devquest

# ===========================================
# Next.js Configuration
# ===========================================
# Base URL for your application
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret_here

# ===========================================
# Background Service Configuration
# ===========================================
# Update interval in milliseconds (default: 5 minutes)
BACKGROUND_UPDATE_INTERVAL_MS=300000

# Batch size for processing users (default: 5)
BACKGROUND_UPDATE_BATCH_SIZE=5

# Delay between batches in milliseconds (default: 2 seconds)
BACKGROUND_UPDATE_BATCH_DELAY_MS=2000

# ===========================================
# Development/Production Settings
# ===========================================
NODE_ENV=development

# ===========================================
# Optional: External Services
# ===========================================
# Add any other service API keys here
# DISCORD_BOT_TOKEN=your_discord_bot_token
# SLACK_WEBHOOK_URL=your_slack_webhook_url

# ===========================================
# Setup Instructions
# ===========================================
# 1. Copy this file to .env.local
# 2. Create a GitHub OAuth App at https://github.com/settings/applications/new
#    - Application name: DevQuest
#    - Homepage URL: http://localhost:3000
#    - Authorization callback URL: http://localhost:3000/api/auth/github/callback
# 3. Generate a Personal Access Token at https://github.com/settings/tokens
#    - Required scopes: read:user, user:email, repo, read:org
# 4. Generate an encryption key: openssl rand -hex 32
# 5. Set up your PostgreSQL database
# 6. Run: npm run dev
